<Window x:Class="calcaot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Calorie Tracker"
        Height="680" Width="420"
        Background="{DynamicResource AppBackground}"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
    <Style x:Key="AddButton" TargetType="Button">
        <Setter Property="Width" Value="34"/>
        <Setter Property="Height" Value="34"/>
        <Setter Property="Background" Value="#007AFF"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="Transparent">
                        <Border x:Name="Inner" Background="{TemplateBinding Background}" CornerRadius="17">
                            <Border.RenderTransform>
                                <TranslateTransform Y="0"/>
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#005FCC"/>
                        </Trigger>
                        <EventTrigger RoutedEvent="Mouse.MouseEnter">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation
                                        Storyboard.TargetName="Inner"
                                        Storyboard.TargetProperty="RenderTransform.Y"
                                        To="-6" Duration="0:0:0.15">
                                        <DoubleAnimation.EasingFunction>
                                            <CubicEase EasingMode="EaseOut"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                        <EventTrigger RoutedEvent="Mouse.MouseLeave">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation
                                        Storyboard.TargetName="Inner"
                                        Storyboard.TargetProperty="RenderTransform.Y"
                                        To="0" Duration="0:0:0.15">
                                        <DoubleAnimation.EasingFunction>
                                            <CubicEase EasingMode="EaseOut"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TransparentButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="Transparent">
                        <Border x:Name="Inner" Background="Transparent">
                            <Border.RenderTransform>
                                <TranslateTransform Y="0"/>
                            </Border.RenderTransform>
                            <ContentPresenter/>
                        </Border>
                    </Border>
                    <ControlTemplate.Triggers>
                        <EventTrigger RoutedEvent="Mouse.MouseEnter">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation
                                        Storyboard.TargetName="Inner"
                                        Storyboard.TargetProperty="RenderTransform.Y"
                                        To="-6" Duration="0:0:0.15">
                                        <DoubleAnimation.EasingFunction>
                                            <CubicEase EasingMode="EaseOut"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                        <EventTrigger RoutedEvent="Mouse.MouseLeave">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation
                                        Storyboard.TargetName="Inner"
                                        Storyboard.TargetProperty="RenderTransform.Y"
                                        To="0" Duration="0:0:0.15">
                                        <DoubleAnimation.EasingFunction>
                                            <CubicEase EasingMode="EaseOut"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="MoonButton" TargetType="Button">
        <Setter Property="Width" Value="38"/>
        <Setter Property="Height" Value="38"/>
        <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="Transparent">
                        <Border x:Name="Inner" Background="{TemplateBinding Background}" CornerRadius="19">
                            <Border.RenderTransform>
                                <TranslateTransform Y="0"/>
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </Border>
                    <ControlTemplate.Triggers>
                        <EventTrigger RoutedEvent="Mouse.MouseEnter">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation
                                        Storyboard.TargetName="Inner"
                                        Storyboard.TargetProperty="RenderTransform.Y"
                                        To="-6" Duration="0:0:0.15">
                                        <DoubleAnimation.EasingFunction>
                                            <CubicEase EasingMode="EaseOut"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                        <EventTrigger RoutedEvent="Mouse.MouseLeave">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation
                                        Storyboard.TargetName="Inner"
                                        Storyboard.TargetProperty="RenderTransform.Y"
                                        To="0" Duration="0:0:0.15">
                                        <DoubleAnimation.EasingFunction>
                                            <CubicEase EasingMode="EaseOut"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Background="{DynamicResource AppBackground}">
        <StackPanel Margin="20,30,20,20">

            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Сегодня" FontSize="28" FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryText}"/>
                <Button Grid.Column="1" Content="🌙"
                        Click="ToggleTheme_Click"
                        Style="{StaticResource MoonButton}"
                        VerticalAlignment="Center"/>
            </Grid>

            <Border Background="{DynamicResource CardBackground}" CornerRadius="20" Padding="20" Margin="0,0,0,14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="140"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <StackPanel Margin="0,0,24,0">
                                <TextBlock Text="{Binding ConsumedCalories, StringFormat={}{0:F0}}"
                                           FontSize="20" FontWeight="Bold"
                                           Foreground="{DynamicResource PrimaryText}"/>
                                <TextBlock Text="Потреблено" Foreground="{DynamicResource SecondaryText}" FontSize="11"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="0" FontSize="20" FontWeight="Bold" Foreground="#FF6B35"/>
                                <TextBlock Text="Сожжено" Foreground="{DynamicResource SecondaryText}" FontSize="11"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>

                    <Canvas Grid.Column="1" Width="130" Height="130">
                        <Ellipse Width="130" Height="130"
                                 Stroke="#E5E5EA" StrokeThickness="10" Fill="Transparent"/>
                        <Path x:Name="ProgressArc"
                              Stroke="#007AFF" StrokeThickness="10"
                              StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                              Fill="Transparent"/>
                        <StackPanel Canvas.Left="20" Canvas.Top="40" Width="90">
                            <TextBlock Text="{Binding RemainingCalories, StringFormat={}{0:F0}}"
                                       FontSize="22" FontWeight="Bold"
                                       HorizontalAlignment="Center" Foreground="#007AFF"/>
                            <TextBlock Text="осталось" FontSize="11"
                                       Foreground="{DynamicResource SecondaryText}"
                                       HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Canvas>
                </Grid>
            </Border>

            <ItemsControl ItemsSource="{Binding MealGroups}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource CardBackground}" CornerRadius="14" Margin="0,0,0,10" Padding="14,12">
                            <Grid VerticalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Button Grid.Column="0"
                                        Tag="{Binding Name}"
                                        Click="MealRow_Click"
                                        Style="{StaticResource TransparentButton}"
                                        HorizontalContentAlignment="Left">
                                    <StackPanel Orientation="Horizontal">
                                        <Border Width="44" Height="44" CornerRadius="12"
                                                Background="{DynamicResource IconBackground}" Margin="0,0,12,0">
                                            <TextBlock Text="{Binding Icon}" FontSize="22"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                                        </Border>
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Name}"
                                                       FontWeight="Bold" FontSize="15"
                                                       Foreground="{DynamicResource PrimaryText}"/>
                                            <TextBlock Text="{Binding Summary}"
                                                       Foreground="{DynamicResource SecondaryText}" FontSize="12"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Button>

                                <Button Grid.Column="1" Content="+"
                                        Tag="{Binding Name}"
                                        Click="AddFood_Click"
                                        Style="{StaticResource AddButton}"
                                        VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </StackPanel>
    </ScrollViewer>
</Window>
